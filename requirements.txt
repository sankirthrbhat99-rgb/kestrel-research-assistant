# Kestrel Multi-Agent Research Assistant — Phase 1 + Phase 2
# Target runtime: Python 3.14 (all pins below resolve to 3.14-compatible wheels).

# Vector store with persistent local storage
chromadb>=1.5.9,<2.0

# Local embeddings (BAAI/bge-small-en-v1.5); pulls in torch + transformers
sentence-transformers>=6.1.0,<7.0

# Dense vector maths
numpy>=2.5.3

# Sparse / lexical retrieval
rank-bm25>=0.2.2

# Environment configuration
python-dotenv>=1.0.1

# Tests
pytest>=8.0

# --- Phase 2: Multi-Agent Workflow ---

# LangGraph for the StateGraph orchestration
langgraph>=0.2.0,<1.0

# LangChain Core (LangGraph dependency; prompt / message primitives)
langchain-core>=0.3.0,<1.0

# --- Phase 2: Observability ---

# LangSmith tracing client (already a transitive dep of langchain-core)
langsmith>=0.1.0
streamlit
